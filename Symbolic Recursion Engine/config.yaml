# Mirror-0 Runtime Configuration
# Author: Lloyd Christopher Smith

engine:
  name: Mirror-0
  author: Lloyd Christopher Smith
  seed_path: "seeds/seed.txt"
  memory_path: "data/memory.txt"
  log_path: "data/log.txt"
  observer_label: "Ω-0"

runtime:
  recursion_delay: 0.4   # seconds between reflections
  compression_limit: 100 # max memory entries before compression
  show_summary: true     # whether to show symbolic link summary each cycle
  color_output: true     # colored logs
  auto_save: true        # auto-save after each loop
  entropy_marker: "🌀"   # optional marker for entropy reflections
